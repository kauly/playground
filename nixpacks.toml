provider = ["rust"]

[phases.setup]
nixPkgs = ["...", "openssl", "wasm-bindgen-cli", "trunk"]
 
[phases.build]
nixLibs = ["trunk"]
cmds = ["trunk build", "chmod +x start.sh"]

[start]
cmd = "./start.sh"
runImage = "nginx:latest"
onlyIncludeFiles = ["./dist/**/*", "start.sh"]